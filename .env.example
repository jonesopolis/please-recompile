# Contentful Configuration
# Copy this file to .env and fill in your actual values

# === Required for Publishing Blog Posts ===
# Your Contentful Space ID (found in Settings → General Settings)
CONTENTFUL_SPACE_ID=your_space_id_here

# Contentful Management API Token (create in Settings → API keys → Content management tokens)
# This token needs write access to create/update entries
CONTENTFUL_MANAGEMENT_TOKEN=your_management_token_here

# === Optional ===
# Contentful Environment (defaults to 'master' if not specified)
CONTENTFUL_ENVIRONMENT=master

# === Required for React App (Reading Posts) ===
# Contentful Delivery API Token (found in Settings → API keys)
# This is used by the React app to fetch and display posts
VITE_CONTENTFUL_ACCESS_TOKEN=your_delivery_api_token_here

# Contentful Preview API Token (optional - for viewing draft posts)
# VITE_CONTENTFUL_PREVIEW_TOKEN=your_preview_api_token_here
